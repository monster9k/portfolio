/** The one overlay transition (duration/easing) used across Tooltip/InfoPanel — mirrors tokens.css --motion-duration/--motion-ease. */
export const OVERLAY_TRANSITION = { duration: 0.22, ease: [0.16, 1, 0.3, 1] as const }
